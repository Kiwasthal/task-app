import styled from 'styled-components';

const StyledInputManager = styled.div`
  padding: 20px;
  background-color: #fff;
  border: 2px solid #ddd;
  grid-area: inputManager;
  display: flex;
`;

export default StyledInputManager;
